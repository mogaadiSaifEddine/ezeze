export interface FieldData {
  showLabel: boolean;
  showCorrectValue: boolean;
  showPlaceholder: boolean;
  showValue: boolean;
  showOrder: boolean;
  labelHolder?: string;
  correctValueHolder?: string;
  placeHolder?: string;
  valueHolder?: string;
}
