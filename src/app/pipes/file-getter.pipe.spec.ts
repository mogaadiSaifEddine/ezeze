import { FileGetterPipe } from './file-getter.pipe';

describe('FileGetterPipe', () => {
  it('create an instance', () => {
    const pipe = new FileGetterPipe();
    expect(pipe).toBeTruthy();
  });
});
